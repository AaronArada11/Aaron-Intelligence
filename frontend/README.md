# Aaron Intelligence Frontend

Vite/React frontend for the Aaron Intelligence portfolio.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run build
```

The dev server proxies `/chat` and `/github-commits` to `http://localhost:8000`.
