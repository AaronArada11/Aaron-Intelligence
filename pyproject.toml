[project]
name = "aaron-intelligence"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi==0.128.8",
  "google-genai==1.14.0",
  "langfuse==4.12.0",
  "pydantic==2.13.4",
  "python-dotenv==1.2.1",
  "requests==2.32.5",
  "supabase==2.31.0",
  "uvicorn==0.39.0",
]

[project.scripts]
app = "api.chat:app"

[tool.vercel]
entrypoint = "api.chat:app"
